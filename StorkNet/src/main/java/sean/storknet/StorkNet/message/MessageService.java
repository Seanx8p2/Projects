package sean.storknet.StorkNet.message;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import org.springframework.stereotype.Service;

//Marks as a business service
@Service
public class MessageService {
	//Data Structure for message here. Possibly ArrayList
	
		
		//iterate over List of Messages and match id with it via lambda 
			public void getMessage(String id ) {
			}

			public void addMessage(Message message) {
				
			}

			public void updateMessage(String id, Message message) {
				//compare each message to the argument and set the updated message to one that matches the old one with id
				
			}

			public void deleteMessage(String id) {
			}
		


}
